server:
  host: "0.0.0.0"
  port: 8000
  workers: 4
  reload: false
rate_limiting:
  default: "100/minute"
  auth: "10/minute"
  heavy: "10/hour"
cors:
  allow_origins: ["*"]
  allow_methods: ["GET", "POST", "PUT", "DELETE"]
  allow_headers: ["*"]
